import java.io.File;
import java.util.ArrayList;

public class Tester {
	public static void main(String[] args) {
		// Model model = new Model();
		// ArrayList<String> arr = model.loadFile(new File("Ascending.dat"));
		//
		// IsearchingAlgorithm isearchingAlgorithm = new InterpolationSearch();
		// int p = isearchingAlgorithm.search(arr, "00TW");
		// int c = isearchingAlgorithm.getNumComparations();
		// System.out.println(c);
		int a = 2, b = 5;
		double d = a / (double) b;
		System.out.println(d);
	}
}
